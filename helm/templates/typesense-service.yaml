apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-typesense
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 8108
      targetPort: 8108
      protocol: TCP
  selector:
    app: {{ $.Release.Name }}-typesense
